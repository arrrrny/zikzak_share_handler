# Publish Configuration for zikzak_share_handler

<!-- Auto-generated by publish-manager skill on 2026-07-10. Edit if the workflow changes. -->

## Package Manager

- **Type**: `pub.dev`
- **Packages** (in publish order):
  1. `zikzak_share_handler_platform_interface`
  2. `zikzak_share_handler_android`
  3. `zikzak_share_handler_ios`
  4. `zikzak_share_handler_macos`
  5. `zikzak_share_handler_linux`
  6. `zikzak_share_handler_web`
  7. `zikzak_share_handler_windows`
  8. `zikzak_share_handler`
- **Public packages**: All 8 packages are published to pub.dev

## Scripts

- **Publish**: `./scripts/publish.sh` — publishes all packages in dependency order, creates git tag
- **Pre-publish**: None (versions and changelogs are updated manually before publishing)

## Workflow

1. Update all `pubspec.yaml` versions to the target version
2. Update all `CHANGELOG.md` files with the new version entry
3. Create and switch to `publish-<version>` branch
4. Commit and push the branch
5. `./scripts/publish.sh`

## Notes

- The publish script is interactive and prompts for confirmation before proceeding
- It automatically handles dependency resolution, dry-run, and publishing for each package in order
- After publishing, it creates and pushes a git tag matching the version
- Referenced scripts that do not yet exist: `push_to_master.sh`, `restore_dev_setup.sh`, `revert_publish_changes.sh`
